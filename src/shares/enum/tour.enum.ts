export enum TourStatus {
  WAITING = `2`,
  INACTIVE = `0`,
  ACTIVE = `1`,
  REJECTED = `3`,
}

export enum GetBy {
  PROVINCE = 'province',
  TOUR_GUIDE = 'tour_guide',
}

export enum AdminApproveAction {
  APPROVE = 'APPROVE',
  REJECT = 'REJECT',
}
